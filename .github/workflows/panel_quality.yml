# DEACTIVATED — apps/panel_flutter_web removed in Faz 5 (Flutter Web → Next.js migration).
# This workflow will never trigger automatically. Kept for historical reference only.
name: Panel Quality (Archived)

on:
  workflow_dispatch:
    inputs:
      _note:
        description: 'Disabled. Flutter web panel replaced by apps/web_next (Next.js).'
        required: false

jobs:
  panel_quality:
    if: false  # never runs — archived
    runs-on: ubuntu-latest
    steps:
      - name: Archived
        run: echo "This workflow is archived. See apps/web_next for the active panel."
